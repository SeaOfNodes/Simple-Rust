fun puts(s: NullTerminatedString) -> I32;

fun main() -> I32 {
    var x: NullTerminatedString = "Hello";
    var y: NullTerminatedString = "World";
    puts(x);
    x = y;
    y = "Bye";
    puts(x);
    puts(y);
    return 0;
}

[package]
name = "simple-rust"
version = "0.1.0"
authors = ["Robert Obkircher <robert.obkircher@gmail.com>"]
edition = "2021"

[features]
fuzzing = []

[dependencies]
# nightly is required for may_dangle on drop
hashbrown = { version = "0.17", features = ["nightly"] }